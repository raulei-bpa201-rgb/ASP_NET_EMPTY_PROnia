@using WebApplicationTASK14.Models
@model List<Slide>
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Bordered table</h4>
            <p class="card-description">
                Add class <code>.table-bordered</code>
            </p>
            <div class="table-responsive pt-3">
                <a asp-controller="slide" asp-action="create" class="btn btn-success m-3">Add Product</a>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>
                                Image
                            </th>
                            <th>
                                ID
                            </th>
                            <th>
                                Title
                            </th>
                            <th>
                                Order
                            </th>
                            <th>
                                Operations
                            </th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (Slide slide in Model)
                        {
                            <tr>
                                <td>
                                    <img src="~/assets/images/website-images/@slide.Image" alt="Alternate Text" />
                                </td>
                                <td>
                                    @slide.Id
                                </td>
                                <td>
                                    @slide.Title
                                </td>
                                <td>
                                    @slide.Order
                                </td>
                                <td>
                                    <a asp-controller="Slide" asp-action="Update" asp-route-id="@slide.Id" class="btn btn-warning">Update</a>
                                    <a asp-controller="Catagory" asp-action="Detail" asp-route-id="@slide.Id" class="btn btn-primary">Detail</a>
                                    <a asp-action="Delete" asp-route-id="@slide.Id" class="btn btn-danger">Delete</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
